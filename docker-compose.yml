services:
  db:
    image: postgres:16-alpine
    container_name: prepai-postgres
    environment:
      POSTGRES_USER: prepai
      POSTGRES_PASSWORD: prepai
      POSTGRES_DB: prepai
    ports:
      - "5432:5432"
    volumes:
      - prepai_pg:/var/lib/postgresql/data

volumes:
  prepai_pg:
