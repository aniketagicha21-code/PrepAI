# Local dev: leave empty (Vite proxies /api to the backend on port 8000).
# Production (Vercel): set in dashboard OR use .env.production — no trailing slash.
VITE_API_URL=https://prepai-iru2.onrender.com
