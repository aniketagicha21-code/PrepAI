# Used only if you build with context=backend (e.g. cd backend && docker build .).
# Render uses repo-root context; see /.dockerignore
.venv
venv
__pycache__
*.py[cod]
*.egg-info
.env
.env.*
.git
.gitignore
*.md
.pytest_cache
.mypy_cache
.ruff_cache
