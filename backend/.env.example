DATABASE_URL=postgresql://prepai:prepai@127.0.0.1:5432/prepai
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o
WHISPER_MODEL=whisper-1
# Comma-separated origins, no trailing slashes. Include local dev + every frontend URL (e.g. Vercel).
# Production example (Render): http://localhost:5173,https://prep-ai-sepia-iota.vercel.app
CORS_ORIGINS=http://localhost:5173,https://prep-ai-sepia-iota.vercel.app
